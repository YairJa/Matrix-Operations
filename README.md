# Matrix Operations in Java

This project implements core matrix operations using two-dimensional arrays of RationalNumber objects for exact rational arithmetic.

Supported features include:

- Matrix multiplication

- Rank calculation (via canonical form)

- Elementary row operations: swap, scale, subtract

Most methods work in place, updating the given matrix directly.
